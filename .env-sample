# ===========================================
# Backend Configuration
# ===========================================

# Server port
PORT=5001

# Firebase Admin SDK service account (JSON string)
# Get this from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Then stringify the JSON: JSON.stringify(serviceAccountJson)
FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"your-project-id",...}'

# Or alternatively, path to service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ===========================================
# Frontend Configuration (for Docker build)
# ===========================================

# Backend API URL - frontend calls this directly from the browser
# For local development: http://localhost:5001
# For production: use your publicly accessible backend URL
VITE_API_URL=http://localhost:5001

# Firebase Client SDK config
# Get these from Firebase Console > Project Settings > General > Your apps
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
